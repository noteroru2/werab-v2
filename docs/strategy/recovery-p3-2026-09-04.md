# RECOVERY P3 — CORE MONEY PAGE RANKING PUSH + TRUST / PROOF / E-E-A-T

Date: 2026-09-04

## Verdict

- Static recovery gate: **PASS**
- Production deploy: **HOLD until dependency install + Astro build/runtime audits pass**
- Controlled URLs: **405**
- INDEX: **86**
- HOLD_NOINDEX: **31**
- DRAFT: **29**
- REDIRECT: **53**
- GONE: **206**

## P3 objective

P3 does not expand local-page volume. It strengthens the six critical commercial owners and creates a visible trust layer around the valuation process.

Core money pages:

1. `/รับซื้อโน๊ตบุ๊ค/`
2. `/รับซื้อคอม/`
3. `/รับซื้อแมคบุ๊ค/`
4. `/รับซื้อไอโฟน/`
5. `/รับซื้อไอแพด/`
6. `/รับซื้อกล้อง/`

## Core money ranking work

A reusable `CoreMoneyAuthority.astro` section is now placed directly after the hero on all six core pages. Each instance contains:

- category-specific valuation factors;
- visible disclosure that online quotes are preliminary;
- final-price confirmation after model/spec/condition/accessory inspection;
- category-specific brand/series/condition support links;
- visible content/valuation reviewer: `คุณโน๊ต`;
- review date: 4 September 2026;
- privacy guidance: no Password, PIN, OTP or Recovery Key needed for a preliminary quote;
- direct links to price-check, trust, about and contact pages.

This creates a direct authority path:

`Core Money Page ↔ Brand/Series/Condition Support ↔ Valuation Guide ↔ Trust/About/Contact`

## Trust / proof release

Two trust pages were rebuilt and promoted:

- `/เกี่ยวกับเรา/`: `HOLD_NOINDEX → INDEX`
- `/ความน่าเชื่อถือ/`: `HOLD_NOINDEX → INDEX`

Both are `READY` and use verifiable process evidence rather than testimonials, fabricated transaction counts, fake branch claims, or unverifiable guarantees.

Two policy pages remain intentionally noindex but were rewritten to remove unsupported claims:

- `/terms/`
- `/privacy-policy/`

## Risky trust claims removed

P3 removes prior statements such as:

- guaranteed/100% data sanitization;
- immediate payment assertions;
- broad legal-compliance guarantees;
- unsupported stolen/pawned-goods claims.

They are replaced with user-verifiable statements about valuation inputs, preliminary-vs-final price, data users should not send, ownership readiness, and consistent contact channels.

## Sitewide trust discovery

- Homepage now renders `TrustBar` immediately after the hero.
- Footer seller-guide column now links price check, valuation standards, about and contact.
- Footer policy navigation links About, Trust, Terms, Privacy and Contact.
- Contact page links directly to valuation standards, operator information, privacy guidance and terms.
- `Organization` schema now exposes `knowsAbout` from the verified accepted-category list and still does not invent an address or LocalBusiness branch.

## URL lifecycle delta

P2 → P3:

- INDEX: `84 → 86`
- HOLD_NOINDEX: `33 → 31`
- DRAFT: `29 → 29`
- REDIRECT: `53 → 53`
- GONE: `206 → 206`
- Controlled URLs: `405 → 405`

No redirect was added or changed in P3.

## URL equity master

`reports/seo/url-equity-master.csv/json` reconciled for:

- `/เกี่ยวกับเรา/`
- `/ความน่าเชื่อถือ/`

Both are now `ACTIVE_INDEX_200 / MAINTAIN_ACTIVE_INDEX` with P3 trust-release evidence. Historical GSC metrics were not fabricated or copied to these pages.

## Static QA

Passed:

```bash
node scripts/content-hygiene-audit.mjs
node scripts/recovery-p0-audit.mjs
node scripts/recovery-p1-audit.mjs
node scripts/recovery-p2-audit.mjs
node scripts/recovery-p3-audit.mjs
```

P3 gate verifies:

- exact lifecycle state;
- all six critical money pages remain INDEX;
- visible category-specific methodology exists on all six;
- visible operator/reviewer disclosure exists;
- support and trust links exist;
- About and Trust are INDEX + READY;
- TrustBar does not contain P3-blocked unverifiable claims;
- homepage/footer/contact expose the trust layer;
- no LocalBusiness schema/address was fabricated;
- P0/P1/P2 regressions remain passing;
- production content-hygiene gate remains clean across pages, Markdown content and reusable components (105 rendered-copy sources).

## Production gate

Before deploy run:

```bash
npm ci
npm run audit:content-hygiene
npm run audit:p0
npm run audit:p1
npm run audit:p2
npm run audit:p3
npm run build
npm run check
npm run audit:redirects
npm run audit:sitemap
npm run audit:links
npm run audit:content-copy
npm run audit:cutover
```

Production verdict must remain `DEPLOY_HOLD_UNTIL_BUILD` until the install/build/runtime sequence passes in an environment with dependencies available.

### Build attempt in this environment

`npm ci --prefer-offline --no-audit --no-fund` was attempted during P3 but the container returned `TransportTimeoutError` before dependencies were available. Partial `node_modules` was removed before packaging, so no incomplete dependency tree is shipped in the release ZIP.
