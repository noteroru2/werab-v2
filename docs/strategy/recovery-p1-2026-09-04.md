# RECOVERY P1 — HISTORICAL WINNER REBUILD + CORE MONEY PAGE AUTHORITY

Date: 2026-09-04

## Verdict

**STATIC PASS / PRODUCTION DEPLOY HOLD** — P1 source architecture and regression gates pass. In this workspace `npm ci` timed out before dependencies completed, so `astro` was unavailable and the production build/runtime gates could not be certified here.

## URL lifecycle after P1

| State | Count |
|---|---:|
| INDEX | 63 |
| HOLD_NOINDEX | 40 |
| DRAFT | 29 |
| REDIRECT | 48 |
| GONE | 206 |
| **Controlled URLs** | **386** |

## Historical equity decisions

- 13 measured `A_PRESERVE_REWRITE` historical IT candidates dispositioned in P1.
- 11 exact historical URLs rebuilt as self-canonical INDEX pages: **69 clicks / 1,133 impressions** in the stored 90-day evidence window.
- 2 weaker duplicate-intent URLs consolidated by one-hop 301: **8 clicks / 284 impressions**.
- Total measured historical clicks explicitly dispositioned by P1: **77 clicks**.
- Measured `A_PRESERVE_REWRITE` candidates left unregistered after P1: **1** (the only remaining record has no measured clicks and is not part of this winner release).

## Exact historical winners released

1. `/รับซื้อเมืองศรีสะเกษ/`
2. `/รับซื้อกล้องมือสอง-บริก/`
3. `/รับซื้อกล้องถ่ายรูปจัง/`
4. `/รับซื้อคอม-ศรีสะเกษ/`
5. `/รับซื้อโน๊ตบุ๊ค-บึงกาฬ/`
6. `/รับซื้อไอแพด-อุบล/`
7. `/รับซื้อโน๊ตบุ๊ค-จังหวัด/` (legacy title identifies Nong Khai intent; public copy does not discuss migration)
8. `/รับซื้อเมืองอุดรธานี/`
9. `/รับซื้อกล้อง-fujifilm-ฟูจิฟิล์ม/`
10. `/รับซื้อโน๊ตบุ๊ค-ศรีสะเ-2/`
11. `/รับซื้อคอม-สายไหม/`

## P1 consolidations

- `/รับซื้อโทรศัพท์-อุบล/` → `/รับซื้อมือถือ-อุบล/`
- `/รับซื้อโน๊ตบุ๊ค-notebook/` → `/รับซื้อโน๊ตบุ๊ค/`

Both targets are INDEX and neither creates a redirect chain.

## Core money authority changes

- Released `/เช็กราคาก่อนขาย/` from HOLD_NOINDEX to INDEX after rewriting it as a real seller-support guide.
- Added contextual links from Notebook, PC, Camera, MacBook, iPhone and iPad core money pages to P1 winners.
- Added links from all six core money pages to `/เช็กราคาก่อนขาย/`.
- Updated cluster-aware internal-link logic to honor curated `relatedPages` before filling same-cluster siblings.
- Added proper hero mapping for MacBook, iPad, smartphone and general-device dynamic content pages.

## Content hygiene and differentiation

- Production-copy hygiene now scans both `.astro` and rendered Markdown bodies.
- It rejects internal migration/search language such as old-URL explanations, search-history notes, template wording and previous SEO editorial terms.
- Removed a pre-existing migration explanation from the Kalasin notebook page and replaced visible `หน้า Local` wording with natural service wording.
- P1 recovered-page maximum character 4-gram Jaccard similarity: **16.1%**, well below the P1 blocker threshold of 35%.

## Static gates

```bash
npm run audit:content-hygiene
npm run audit:p0
npm run audit:p1
```

All three static gates pass in the recovery workspace. Dependency installation did not complete in this environment; `npm run build` therefore returned `astro: not found`. The partial `node_modules` directory was removed before packaging.

## Production release gate

Before deploy, run:

```bash
npm ci
npm run audit:content-hygiene
npm run audit:p0
npm run audit:p1
npm run build
npm run check
npm run audit:redirects
npm run audit:sitemap
npm run audit:links
npm run audit:content-copy
npm run audit:cutover
```

Do not submit the new sitemap or request reindexing until the production HTML/status/canonical output matches this P1 source state.
